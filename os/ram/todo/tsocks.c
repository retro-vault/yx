/*
 *	tsocks.c
 *	tiny sockets interface is a lightweight interface enabling 
 *	zx spectrum 48K to connect to internet via interface 1 / rs232.
 *	
 *	tomaz stih thu jul 26 2012
 */
#include "tsocks.h"

result connect(socket_t* s, string address, word port) {
	
}

void close(socket_t *s) {
}

result send(socket_t* s, byte* buffer, word length) {
}

result recv(socket_t* s, byte* buffer, word length, word *rlength) {
}



#endif